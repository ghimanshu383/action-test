name: Shell commands

on: [push]

jobs:
  run-shell-commands:
    runs-on: ubuntu-18.04
    steps:
      - name: echo a string
        run: echo "Hello world"
      - name: Run multiple commands
        run: |
          node -v
          npm -v


